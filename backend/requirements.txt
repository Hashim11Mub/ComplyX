fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-docx>=1.1.0
anthropic>=0.40.0
qdrant-client>=2.0.0
sentence-transformers>=3.3.0
pymupdf>=1.24.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
langsmith>=0.1.0
httpx>=0.27.0

# Phase 1 (LangGraph agent) — install when starting Phase 1:
# langchain-anthropic>=0.3.0
# langgraph>=0.2.0

# RAG evaluation (install before running tests/eval_run.py):
# ragas>=0.2.0
# datasets>=3.0.0

# Optional: heavy OCR for scanned Arabic PDFs (skip for born-digital SAMA docs)
# docling>=2.0.0
